# SmartFarm Core

A lightweight core service for SmartFarm IoT automation.  
Before running, make sure you already have an MQTT broker running (e.g. **Mosquitto**).

---

## Requirements

- MQTT server (Mosquitto, EMQX, HiveMQ, etc.)
- Bun ≥ **1.2.23**

After all:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.2.23. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
